<ul>
    <% @movies.each do |movie| %>
    <li>
    <strong><%= movie.title %></strong>
    <%= movie.rating%>:
    <%= total_gross(movie)%>
    <%= number_to_currency(movie.total_gross, precision: 0) %>
    <%end%>
</ul>